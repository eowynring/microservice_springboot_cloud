spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/goffyguo/spring-cloud-config
          username: goffyguo
          password:
server:
  port: 12580